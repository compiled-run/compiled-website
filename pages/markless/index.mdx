import CounterDemo from '../../components/demos/CounterDemo.tsrx';

# Markless

Markless builds websites and apps out of components you write in TypeScript.

The idea it is built around is small. Some of your variables are ones the page follows. You read
them and assign to them like any other variable, and whenever one changes, the parts of the page
that used it change with it.

Here is that, running on this page. Click it.

<CounterDemo />

The next page shows how that button is written, and it is shorter than you expect:
[State: a variable the UI watches](/markless/concepts/state).
