import PageMeta from '../../../components/page-meta.tsrx';
import Collapsible from '../../../components/demos/collapsible.tsrx';
import Callout from '../../../components/demos/callout.tsrx';
import ThemeToggle from '../../../components/docs/theme-toggle.tsrx';
import LikeHeart from '../../../components/docs/like-heart.tsrx';

# A key tells the page which row is which

<PageMeta level="Start here" time="6 min" assumes="state, conditionals" sprite="dashes" />

<ThemeToggle />

Everybody ships this bug once. A table with a checkbox on every row. You tick one, you sort the
table, and the tick is now on somebody else's row. Nothing threw. Nothing looked broken. The wrong
person got the email.

The page had no idea the rows were the same rows in a new order. It saw three slots. It filled them
again from the top.

**A key is the answer to "which row is this?", and once a row has one, everything attached to that
row moves with it.**

## Tick the middle row, then sort

Three rows, one checkbox each, and a button that sorts them alphabetically. Put the file below in
your own project. Tick the middle row, Pears, then press Sort. Watch where the word "picked" ends
up.

<Callout sticker="map" sprite="cross" title="Why there is no demo box on this page yet" text="The rows below are meant to be a box you can tick and sort. They are not on the page yet. A component using @for does not compile on the framework build this site is pinned to: the compiler stops making progress and the build never finishes. The file below is real, and it is what you will run in your own project. The page comes back with its widget the day the build accepts it. NOTES.md finding 23 in the site repository has the reproduction." />

Pears will move, and the tick will move with it. Nothing in the code copies a checkbox state from
one row to another, because nothing needs to. The row is a thing with a name, and the name did not
change when its position did.

Here is the file:

```tsrx
import { state } from '@markless/core';

export default function SortableRows() @{
	let rows = state([
		{ id: 'pears', label: 'Pears', picked: false },
		{ id: 'apples', label: 'Apples', picked: false },
		{ id: 'plums', label: 'Plums', picked: false },
	]);

	<section>
		<ul class="row-list">
			@for (const row of rows; key row.id) {
				<li class="row">
					<label>
						<input
							type="checkbox"
							checked={row.picked}
							onChange={() => (row.picked = !row.picked)}
						/>
						{row.label}
					</label>
					<span class="row-mark">{row.picked ? 'picked' : ''}</span>
				</li>
			}
		</ul>
		<button
			onClick={() =>
				(rows = [...rows].sort((left, right) => left.label.localeCompare(right.label)))
			}
		>
			Sort A to Z
		</button>
	</section>
}
```

The whole of the identity story is `key row.id` in the loop header.

## What a key buys you

A lot travels with a keyed row. Its component instances, its local `state()`, its derived values,
its async work, its DOM updates and its event wiring. All of it stays attached to the same logical
item across reorder, insert, and delete operations.

So a key is not a hint for a diffing algorithm to do better. It is the identity of the row. It is
what makes a row a place where state can live at all.

## Choosing the key

Use the thing that identifies the item in your data. A database id, a slug, an order number. It has
to be stable, meaning the same item carries the same key on every render. It has to be unique
within the list.

Two things not to use. Do not use the array index by accident: it names the slot rather than the
item, which is the original bug in a new costume. Do not use a value the user can edit. A row that
gets renamed becomes, as far as the page is concerned, a different row.

Naming the position is sometimes genuinely what you want, and TSRX has a way to say so out loud:

```tsrx
import ProductCard from './product-card.tsrx';
import type { Product } from './products.ts';

export default function ProductGrid({ products }: { readonly products: readonly Product[] }) @{
	<section>
		@for (const product of products; index i; key i) {
			<ProductCard product={product} />
		}
	</section>
}
```

That reads "state follows the slot, not the item". It is right for a fixed set of positions and
wrong for a list that reorders.

<Callout sprite="cross" title="What if I just leave the key off?" text="An unkeyed loop is positional. That is fine for output that never changes and holds nothing, a list of static labels, say. The moment a row holds state or handles events, the compiler asks for a key and points at the @for header. There are three diagnostics. MARKLESS_REPEAT_KEY_REQUIRED means there is no key on a stateful loop. MARKLESS_REPEAT_KEY_IS_INDEX means the key is the index and the list can reorder. MARKLESS_REPEAT_KEY_UNSTABLE means the key is something that can change under you. You find out at build time, not from a support ticket." />

## An empty list gets its own branch

You do not need an `@if` around the loop to say "nothing here yet". `@empty` belongs to the loop
and renders when it has no items:

```tsrx
import { state } from '@markless/core';

export default function Updates() @{
	let updates = state([{ id: 'markless', project: 'Markless', version: '0.3.1' }]);

	<ul class="update-list">
		@for (const update of updates; key update.id) {
			<li>
				<strong>{update.project}</strong>
				<span>{update.version}</span>
			</li>
		} @empty {
			<li>No local updates</li>
		}
	</ul>
}
```

## Why the sort was cheap

Reordering a keyed list moves existing rows. It does not rebuild them, and it does not run any
component function again. The rows already exist, keyed, and the work is moving DOM nodes into a
new order.

It is one rule at a larger scale. Changing a number
touched one piece of text. Sorting a list touched the order of some rows. Neither one re-ran your
code.

<Collapsible sprite="star-badge" summary="Coming from another framework?" text="Keys are often described as a performance hint, and index keys as a small sin to get away with in a static list. The framing here is different and stricter. A key is the identity root for the row's own graph scope. That is to say it is what makes local state inside a row possible in the first place. It is also why the compiler refuses to guess: an interactive loop without a stable key is a build error, not a warning in a console you may never open." />

## Try it yourself

Give each row its own counter with a button that increases it. Tick nothing, and sort. The counts
follow their rows. Then change `key row.id` to `key i`, using an `index i` clause, and do it again.
The counts stay behind while the labels move. That gap is the whole of what a key is for.

Next: [how a page waits for data](/markless/concepts/async) without you ever writing a loading flag.

<LikeHeart />
